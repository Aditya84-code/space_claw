# ──────────────────────────────────────────────
# Space Claw — Environment Variables
# Copy this file to .env and fill in real values.
# NEVER commit .env to git.
# ──────────────────────────────────────────────

# Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN=
ALLOWED_USER_ID=

# Default Model
# OpenAI API Key
OPENAI_API_KEY=
# The model to use (e.g. gpt-4o, gpt-4o-mini)
OPENAI_MODEL=gpt-4o

# Max tool-call iterations per agent turn (safety cap — default 10)
AGENT_MAX_ITERATIONS=10

# Log level: debug | info | warn | error
LOG_LEVEL=info

# Database path
MEMORY_DB_PATH=./data/memory.db

# ── Web Search (Tavily) ─────────────────────────────────────────────────────
# Get a free key at https://app.tavily.com (1,000 credits/month free)
TAVILY_API_KEY=

# ── Text-to-Speech (ElevenLabs) ──────────────────────────────────────────────────
# Set to true to send voice messages after every reply
TTS_ENABLED=false
# Your ElevenLabs API key (from elevenlabs.io/app/api-key)
ELEVENLABS_API_KEY=
# Voice ID to use (browse at elevenlabs.io/voices — default: Rachel)
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# Transcribe incoming Telegram voice messages via Whisper and respond with voice
VOICE_TRANSCRIPTION_ENABLED=true

# ── Supabase Semantic Memory (pgvector) ─────────────────────────────────────
# Create a free project at https://supabase.com → Project Settings → API
# Run the schema SQL from ./data/superbase-scheme.sql once in the Supabase SQL Editor
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=


# Set to false to disable semantic memory and use SQLite FTS5-only recall
SEMANTIC_MEMORY_ENABLED=true

# ── Multi-LLM Providers ─────────────────────────────────────────────────────
# Hot-swap with /model command. Only fill keys for providers you use.

# Anthropic Claude — https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Google Gemini — https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=
# DeepSeek — https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=

# Groq (fast inference) — https://console.groq.com/keys
GROQ_API_KEY=

# Command shell
# Folders that space claw can access
FILE_ALLOWED_PATHS=./data,./skills
FILE_MAX_SIZE_BYTES=1048576

# Zapier MCP
ZAPIER_MCP_API_KEY=